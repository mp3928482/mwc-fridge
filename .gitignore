# ── Secrets ────────────────────────────────────────────
data/config.json          # wifi creds & fridge ID — NEVER commit this
data/config-*.json        # ignore any other config files (e.g. for different environments)

# ── PlatformIO build artifacts ──────────────────────────
.pio/
.vscode/.browse.vc.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
